package sort;

/**
 * 执行流程：
 *  1. 轴点元素(pivot)，每次假设选择0位置的元素为pivot
 *  2. 将小于pivot的元素放在pivot左侧
 *     大于pivot的元素放在pivot右侧
 *  3. 对子序列进行1,2操作，直到不能分割
 *
 *  快排的本质： 将每个元素都转换为轴点元素
 */
public class QuickSort {

    public static void sort(Comparable[] arr) {

    }
}
